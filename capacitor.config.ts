import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eu.raulmorales.organi',
  appName: 'organi',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
